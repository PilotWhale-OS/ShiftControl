spring:
  jackson:
    default-property-inclusion: non_null
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://keycloak:8080/realms/dev
logging:
  level:
    root: INFO

keycloak:
  auth-server-url: http://keycloak:8080/
  auth-realm: dev
  allowed-issuers:
    - http://keycloak.shiftcontrol.example.com/realms/dev
    - http://keycloak:8080/realms/dev
  client:
    id: admin-cli
    realm: master
    username: admin
    password: admin
